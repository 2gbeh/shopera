import { TryTable } from "./_try/try-table";
import { TryForm } from "./_try/try-form";

export const metadata = { title: "Try React Hook Form" };

export default function Try() {
  return (
    <main>
      {/* <TryForm /> */}
      <TryTable />
    </main>
  );
}
